function update(elapsed:Float) {
      trace("shhh");
      if(PlayState.instance.accuracy <= 85) {
            trace("CakeAtStake");    
      } else {
            trace("You are not up for elemination");
      }
}