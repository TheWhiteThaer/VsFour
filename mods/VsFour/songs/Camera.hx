import funkin.game.PlayState;
function update(elapsed) {
      switch(curStage) {
            case "four":
                  //FlxG.camera.zoom = 0.5;
            default:
                  
      }
}